# DEEPSEEK_API_KEY: Required for AI copywriting optimization.
# This is set at runtime by the user through the UI (key icon in toolbar).
# The key is stored only in browser localStorage and never uploaded to any server.
# Users can get their key at: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY="your_deepseek_api_key_here"
